/// The subgame number.
typedef SubgameNumber = int;

/// Reflects the turn number during a round, i.e. in [0, playerNum-1]
typedef TurnNumber = int;

/// Reflects the rank the given player achieved, where 1 is the best.
typedef PlayerRank = int;
